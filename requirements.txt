oandapyV20
alpaca-py
pandas
pandas-ta
numpy
plotly
dash
scikit-learn
joblib
pygame-ce       # pygame community edition — Python 3.12+ wheels available
# pygame        # fallback if pygame-ce unavailable (may require Visual C++)
python-dotenv
apscheduler
pydub
requests           # Telegram alerts HTTP client
xgboost            # gradient-boosted ML classifier for PatternLearner
pyyaml             # configs/*.yaml -> config.py overrides, adaptive-strategy path only
                    # (config.py degrades gracefully if this isn't installed yet — see
                    # config.py's _load_yaml_overrides())
